/**
 * Intent: Web configuration interface for InfoBento devices
 * Context: Users configure bento boxes, preview layouts, pair devices via Web Bluetooth
 * Pattern: Vite + React SPA — imports types from @infobento/core
 * Future: Drag-and-drop bento editor, live preview via API, device pairing
 */

export const APP_NAME = 'InfoBento';
